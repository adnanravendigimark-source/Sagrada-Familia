// The "Combine with Other Gaudí Sites" homepage section was removed per
// request (no longer imported from app/page.tsx). This file can't be
// deleted from here, so its content has been emptied out rather than left
// as unused dead code.
export default function ComboOffers() {
  return null;
}
