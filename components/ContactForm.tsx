// Unused — /contact uses a direct mailto: link instead (see
// app/contact/page.tsx). This form was never wired to a backend and isn't
// imported anywhere. This file can't be deleted from here, so its content
// has been emptied out rather than left as unused dead code.
export default function ContactForm() {
  return null;
}
