// The Combo Offers admin editor was removed per request (no longer
// reachable — /admin/combos and /api/admin/combos are both disabled). This
// file can't be deleted from here, so its content has been emptied out
// rather than left as unused dead code.
export default function CombosForm() {
  return null;
}
